<script>
	import Sub from './Sub.poly';
	export let selected;
	let banana = {};
	let component = banana;
	$: selected ? component = Sub : component = banana;
</script>

<svelte:component this={component} />
